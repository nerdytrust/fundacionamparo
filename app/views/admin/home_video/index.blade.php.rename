@extends("crud.layout-index")

	@section("table")

		<table class="table table-striped table-bordered ">
			<thead>
				<tr>

					<td> {{ $columns->id_home_video->label }} </td>
					<td> {{ $columns->cover->label }} </td>
					<td> {{ $columns->video->label }} </td>
					<td> {{ $columns->activo->label }} </td>


					@include("crud.index-column-buttons")

				</tr>
			</thead>
			<tbody>

			
			@foreach($records as $record)
				<tr>


					<td>{{ parseToHTML($columns->id_home_video,$record,$fk_column) }}</td>
					<td>{{ parseToHTML($columns->cover,$record,$fk_column) }}</td>
					<td>{{ parseToHTML($columns->video,$record,$fk_column) }}</td>
					<td>{{ parseToHTML($columns->activo,$record,$fk_column) }}</td>


					@include("crud.index-buttons")

				</tr>
			@endforeach



			</tbody>
		</table>

	@stop